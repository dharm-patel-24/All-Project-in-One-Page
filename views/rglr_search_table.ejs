<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Input your Query</title>
    <style>
        .currentPage {
            height: 32px;
            padding: 7px 10px;
            border: none;
        }
        button {
            height: 32px;
            padding: 7px 10px;
            border: none;
            font-size: large;
            background-color: rgb(174, 255, 255);
        }
        button:hover {
            background-color: rgb(133, 239, 239);
        }
        .button {
            background-color: rgb(174, 255, 255);
            font-size: medium;

        }
        .button:hover {
            background-color: rgb(133, 239, 239);
        }
        th, td {
            padding: 5px;
            border-color: rgb(55, 54, 52);
        }
    </style>
</head>

<body style="background-color: rgb(243, 230, 230);">
    <br><br>
    <form method="post" action="/rglr-search-data?page=1" style="text-align: center; margin: 10 auto;">
        <input type="text" name="str" placeholder="Enter your Query" style="height: 30px; width: 330px; border: none;">
        <input type="submit" class="button" value="Go" style="height: 32px; padding: 7px 20px; border: none; ">
    </form>
    <br><br>

    <% if(display == "show") { %>  
    <%- include('rglr_search_component') %>

    <br><br>

    <% try { %>
    <table border="1" style="text-align: center; margin: 0 auto;  border-collapse: collapse; border-color: rgb(55, 54, 52); color: rgb(55, 54, 52);">
        <tr>
            <% col.forEach((data)=> { %>
                <th><%= data %></th>
            <% }) %>
        </tr>
        <% for(i in row) { %>
            <tr>
            <% for(item in col) { %>
                <td><%= row[i][col[item]] %></td>
            <% } %>
            </tr>
        <% } %>
    </table>   
    <% } catch(err) { %>
        <% } %>
    <% } %>
</body>
</html>